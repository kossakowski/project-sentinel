"""Article Dashboard -- a local read-only web app over Project Sentinel's DB.

A Flask API plus (in later phases) a React SPA for browsing, searching, and
analysing the articles, classifications, events, and alerts that the
production monitoring pipeline produces. Runs entirely locally; the production
server is only ever read from.
"""
